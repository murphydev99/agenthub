import React from 'react';
import { useAuth } from '../../contexts/AuthContext';
import { Loader2, Shield, Users, Headphones } from 'lucide-react';

export function B2CLoginPage() {
  const { login, isLoading } = useAuth();

  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-50 to-white">
      <div className="flex min-h-screen">
        {/* Left side - Branding and Features */}
        <div className="hidden lg:flex lg:w-1/2 bg-indigo-600 text-white p-12 flex-col justify-between">
          <div>
            <h1 className="text-4xl font-bold mb-2">AgentHub Support Portal</h1>
            <p className="text-indigo-200 text-lg">Dunkin' / Baskin-Robbins Franchisee Support</p>
          </div>
          
          <div className="space-y-8">
            <div className="flex items-start space-x-4">
              <Shield className="h-8 w-8 text-indigo-300 flex-shrink-0 mt-1" />
              <div>
                <h3 className="font-semibold text-lg mb-1">Secure Access</h3>
                <p className="text-indigo-200">Multi-factor authentication protects your franchisee data</p>
              </div>
            </div>
            
            <div className="flex items-start space-x-4">
              <Users className="h-8 w-8 text-indigo-300 flex-shrink-0 mt-1" />
              <div>
                <h3 className="font-semibold text-lg mb-1">Store-Based Access</h3>
                <p className="text-indigo-200">View and manage tickets for your specific stores</p>
              </div>
            </div>
            
            <div className="flex items-start space-x-4">
              <Headphones className="h-8 w-8 text-indigo-300 flex-shrink-0 mt-1" />
              <div>
                <h3 className="font-semibold text-lg mb-1">AI-Powered Support</h3>
                <p className="text-indigo-200">Get instant answers with intelligent chatbot assistance</p>
              </div>
            </div>
          </div>

          <div className="text-indigo-200 text-sm">
            © 2025 AgentHub. Supporting 10,000+ franchisees nationwide.
          </div>
        </div>

        {/* Right side - Login Form */}
        <div className="flex-1 flex items-center justify-center px-4 sm:px-6 lg:px-8">
          <div className="max-w-md w-full">
            <div className="text-center">
              <div className="mx-auto h-16 w-16 bg-indigo-600 rounded-full flex items-center justify-center mb-6">
                <Shield className="h-8 w-8 text-white" />
              </div>
              
              <h2 className="text-3xl font-bold text-gray-900 mb-2">
                Welcome Back
              </h2>
              <p className="text-gray-600 mb-8">
                Sign in with your franchisee account to access support resources
              </p>
            </div>

            <div className="space-y-4">
              <button
                onClick={login}
                disabled={isLoading}
                className="w-full flex justify-center items-center px-4 py-3 border border-transparent text-base font-medium rounded-lg text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
              >
                {isLoading ? (
                  <>
                    <Loader2 className="mr-2 h-5 w-5 animate-spin" />
                    Connecting to Azure AD B2C...
                  </>
                ) : (
                  <>
                    <Shield className="mr-2 h-5 w-5" />
                    Sign in with Azure AD B2C
                  </>
                )}
              </button>

              <div className="relative">
                <div className="absolute inset-0 flex items-center">
                  <div className="w-full border-t border-gray-300"></div>
                </div>
                <div className="relative flex justify-center text-sm">
                  <span className="px-2 bg-white text-gray-500">Secure authentication powered by Microsoft</span>
                </div>
              </div>

              <div className="bg-blue-50 border border-blue-200 rounded-lg p-4">
                <p className="text-sm text-blue-800">
                  <strong>First time signing in?</strong> You'll be prompted to set up multi-factor authentication (MFA) for enhanced security.
                </p>
              </div>

              <div className="text-center text-sm text-gray-600">
                Having trouble signing in?{' '}
                <a href="/support" className="font-medium text-indigo-600 hover:text-indigo-500">
                  Contact Support
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}